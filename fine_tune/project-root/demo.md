1. **Terminal:**
   ```bash
   python agent.py "Create a new Git branch and switch to it"
2. Narrate:

"This is a fine-tuned LLM-based CLI agent. It takes natural language input and generates step-by-step instructions."

3. Show Output:

Shell commands dry-runned via echo

4. Each step printed and also saved to logs/trace.jsonl

Navigate to log file:

Open logs/trace.jsonl

Show the input + output JSON logs

5. Optionally show dataset or training code

6. Wrap up:

"This was trained on ~150 examples using LoRA and TinyLlama in under 5 hours. Thanks!"